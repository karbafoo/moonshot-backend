const BallotController = require('./ballot');
const ParticipantController = require('./participant');
const VoteController = require('./vote');

module.exports = {
    BallotController,
    ParticipantController,
    VoteController,
};
